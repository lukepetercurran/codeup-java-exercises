package KitchenApp;

// Using the "interface" keyword to create an interface.

// All methods are public and abstract, meaning the implementation needs to be defined in the class that "implements" the interface.

interface StoreFood {
    void keepFresh();

    // Static methods are possible, but are frowned upon.
//    static void sayHello() {
//        System.out.println("Hello");
//    };
}

